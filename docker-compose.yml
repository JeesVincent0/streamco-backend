version: '3.9'

services:
  redis:
    image: redis:7-alpine
    container_name: streamco-redis
    restart: always
    ports:
      - "6379:6379"
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --appendonly yes
    volumes:
      - redis_data:/data
    env_file:
      - .env

volumes:
  redis_data:
